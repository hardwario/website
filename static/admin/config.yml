backend:
  name: git-gateway
  branch: main
media_folder: static/upload
public_folder: /upload
slug:
  encoding: "ascii"
  clean_accents: true
i18n:
  # Required and can be one of multiple_folders, multiple_files or single_file
  # multiple_folders - persists files in `<folder>/<locale>/<slug>.<extension>`
  # multiple_files - persists files in `<folder>/<slug>.<locale>.<extension>`
  # single_file - persists a single file in `<folder>/<slug>.<extension>`
  structure: multiple_files

  # Required - a list of locales to show in the editor UI
  locales: [en, cs]

  # Optional, defaults to the first item in locales.
  # The locale to be used for fields validation and as a baseline for the entry.
  default_locale: en
media_library:
  name: cloudinary
  config:
    cloud_name: lukasfabik
    api_key: 818759917278117
collections:
  - name: 'blog'
    label: 'Blog'
    folder: 'content/blog'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Draft', name: 'draft', widget: 'boolean', default: false, required: false }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Meta title', name: 'meta_title', widget: 'string' }
      - { label: 'Meta description', name: 'meta_description', widget: 'string' }
      - { label: 'Preview image', name: 'image_preview', widget: 'image', required: false, tagname: '' }
      - { label: 'Main image', name: 'image_main', widget: 'image', required: false, tagname: '' }
      - { label: 'Meta image', name: 'image_meta', widget: 'image', required: false, tagname: '' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
  - name: 'tutorials'
    label: 'Tutorials'
    folder: 'content/education/tutorials'
    create: true
    slug: '{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Draft', name: 'draft', widget: 'boolean', default: false, required: false }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Slug', name: 'slug', widget: 'string' }
      - { label: 'Weight', name: 'weight', widget: 'number', default: 0, value_type: 'int' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Meta title', name: 'meta_title', widget: 'string' }
      - { label: 'Meta description', name: 'meta_description', widget: 'string' }
      - { label: 'Preview image', name: 'image_preview', widget: 'image', required: false, tagname: '' }
      - { label: 'Main image', name: 'image_main', widget: 'image', required: false, tagname: '' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
  - name: 'projects'
    label: 'Projects'
    folder: 'content/education/projects'
    create: true
    slug: '{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Draft', name: 'draft', widget: 'boolean', default: false, required: false }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Slug', name: 'slug', widget: 'string' }
      - { label: 'Weight', name: 'weight', widget: 'number', default: 0, value_type: 'int' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Meta title', name: 'meta_title', widget: 'string' }
      - { label: 'Meta description', name: 'meta_description', widget: 'string' }
      - { label: 'Preview image', name: 'image_preview', widget: 'image', required: false, tagname: '' }
      - { label: 'Main image', name: 'image_main', widget: 'image', required: false, tagname: '' }
      - { label: 'Body', name: 'body', widget: 'markdown' }